# KCP 명령 목록

## 셸 전환
- `시엠디` = `__win_cmd__`: Windows CMD 셸을 엽니다.
- `파워셸` = `powershell`: PowerShell 셸을 실행합니다.

## 시스템·콘솔·배치
- `기록검증` = `verify`: 명령을 실행합니다.
- `깨긋` = `cls`: 화면을 지웁니다.
- `날짜` = `date /t`: 현재 날짜를 표시합니다.
- `대기` = `timeout`: 지정한 시간만큼 대기합니다.
- `도와줘` = `help`: 도움말을 표시합니다.
- `도움` = `help`: 도움말을 표시합니다.
- `도움말` = `help`: 도움말을 표시합니다.
- `매개이동` = `shift`: 명령을 실행합니다.
- `명령기록` = `doskey`: 명령을 실행합니다.
- `문자찾기` = `find`: 텍스트 문자열을 검색합니다.
- `문자출력` = `echo`: 문자열을 출력합니다.
- `반복` = `for`: 명령을 실행합니다.
- `버전` = `ver`: Windows 버전을 표시합니다.
- `비교` = `fc`: 파일을 비교합니다.
- `사용자` = `whoami`: 현재 사용자 정보를 표시합니다.
- `색` = `color`: 콘솔 색상을 변경합니다.
- `색상` = `color`: 콘솔 색상을 변경합니다.
- `시간` = `time /t`: 현재 시간을 표시합니다.
- `시스템` = `systeminfo`: 시스템 정보를 표시합니다.
- `시스템정보` = `systeminfo`: 시스템 정보를 표시합니다.
- `실행` = `start`: 프로그램이나 명령을 새 창에서 실행합니다.
- `어디` = `where`: 실행 파일의 위치를 찾습니다.
- `에코` = `echo`: 문자열을 출력합니다.
- `이동표` = `goto`: 명령을 실행합니다.
- `일시정지` = `pause`: 명령을 실행합니다.
- `작업` = `tasklist`: 실행 중인 작업 목록을 표시합니다.
- `작업목록` = `tasklist`: 실행 중인 작업 목록을 표시합니다.
- `작업종료` = `taskkill`: 프로세스나 앱을 종료합니다.
- `장치설정` = `mode`: 명령을 실행합니다.
- `전원` = `shutdown`: 시스템 종료/재시작을 수행합니다.
- `정렬` = `sort`: 명령을 실행합니다.
- `정리` = `cls`: 화면을 지웁니다.
- `조건` = `if`: 명령을 실행합니다.
- `종료` = `taskkill`: 프로세스나 앱을 종료합니다.
- `주석` = `rem`: 명령을 실행합니다.
- `중단검사` = `break`: 명령을 실행합니다.
- `지역끝` = `endlocal`: 명령을 실행합니다.
- `지역시작` = `setlocal`: 명령을 실행합니다.
- `창제목` = `title`: 명령을 실행합니다.
- `찾기` = `findstr`: 파일에서 문자열을 검색합니다.
- `출력` = `echo`: 문자열을 출력합니다.
- `코드페이지` = `chcp`: 명령을 실행합니다.
- `클립` = `clip`: 입력을 클립보드로 보냅니다.
- `파일비교` = `comp`: 파일을 비교합니다.
- `프로세스목록` = `tasklist`: 실행 중인 작업 목록을 표시합니다.
- `프롬프트` = `prompt`: 명령을 실행합니다.
- `호스트` = `hostname`: 호스트 이름을 표시합니다.
- `호출` = `call`: 명령을 실행합니다.
- `화면정리` = `cls`: 화면을 지웁니다.
- `화면지우기` = `cls`: 화면을 지웁니다.
- `환경` = `set`: 환경 변수를 표시/설정합니다.
- `환경변수` = `set`: 환경 변수를 표시/설정합니다.

## 파일·디렉터리·디스크
- `경로` = `cd`: 현재 디렉터리를 표시하거나 변경합니다.
- `경로변경` = `cd`: 현재 디렉터리를 표시하거나 변경합니다.
- `경로설정` = `path`: 명령을 실행합니다.
- `경로저장` = `pushd`: 명령을 실행합니다.
- `고급복사` = `robocopy`: 고급 파일/디렉터리 복사를 수행합니다.
- `나무` = `tree`: 디렉터리 구조를 표시합니다.
- `내용` = `type`: 명령을 실행합니다.
- `대량복사` = `xcopy`: 디렉터리 트리를 포함해 파일을 복사합니다.
- `더보기` = `more`: 명령을 실행합니다.
- `드라이브연결` = `subst`: 명령을 실행합니다.
- `디렉터리` = `chdir`: 현재 디렉터리를 표시하거나 변경합니다.
- `디렉터리목록` = `dir`: 디렉터리 목록을 표시합니다.
- `디렉터리삭제` = `rmdir`: 디렉터리를 삭제합니다.
- `디렉터리생성` = `mkdir`: 디렉터리를 만듭니다.
- `디렉터리이동` = `cd`: 현재 디렉터리를 표시하거나 변경합니다.
- `링크` = `mklink`: 명령을 실행합니다.
- `만들기` = `mkdir`: 디렉터리를 만듭니다.
- `목록` = `dir`: 디렉터리 목록을 표시합니다.
- `바꾸기` = `replace`: 명령을 실행합니다.
- `복구` = `recover`: 명령을 실행합니다.
- `복사` = `copy`: 파일을 복사합니다.
- `볼륨` = `vol`: 명령을 실행합니다.
- `볼륨이름` = `label`: 명령을 실행합니다.
- `삭제` = `del`: 파일을 삭제합니다.
- `속성` = `attrib`: 파일 속성을 표시/변경합니다.
- `압축` = `compact`: 명령을 실행합니다.
- `이동` = `move`: 파일 또는 폴더를 이동합니다.
- `이름바꾸기` = `rename`: 파일 이름을 바꿉니다.
- `이름변경` = `ren`: 파일 이름을 바꿉니다.
- `이전경로` = `popd`: 명령을 실행합니다.
- `인쇄` = `print`: 명령을 실행합니다.
- `지우기` = `rmdir`: 디렉터리를 삭제합니다.
- `트리` = `tree`: 디렉터리 구조를 표시합니다.
- `파일복사` = `copy`: 파일을 복사합니다.
- `파일삭제` = `del`: 파일을 삭제합니다.
- `파일시스템변환` = `convert`: 명령을 실행합니다.
- `파일이동` = `move`: 파일 또는 폴더를 이동합니다.
- `파일이름변경` = `ren`: 파일 이름을 바꿉니다.
- `파일지우기` = `erase`: 파일을 삭제합니다.
- `포맷` = `format`: 명령을 실행합니다.
- `폴더삭제` = `rmdir`: 디렉터리를 삭제합니다.
- `폴더생성` = `mkdir`: 디렉터리를 만듭니다.
- `폴더이동` = `cd`: 현재 디렉터리를 표시하거나 변경합니다.

## 네트워크·관리
- `경로추적` = `tracert`: 목적지까지의 경로를 추적합니다.
- `경로표` = `route`: 명령을 실행합니다.
- `관리명령` = `wmic`: WMI 관련 명령을 실행합니다.
- `권한` = `icacls`: 파일/폴더 ACL을 표시하거나 변경합니다.
- `네트설정` = `netsh`: 네트워크 구성을 관리합니다.
- `네트연결` = `net use`: 네트워크 드라이브 연결을 관리합니다.
- `네트워크` = `ipconfig`: 네트워크 구성 정보를 표시합니다.
- `네트워크설정` = `ipconfig`: 네트워크 구성 정보를 표시합니다.
- `네트워크정보` = `ipconfig`: 네트워크 구성 정보를 표시합니다.
- `드라이버` = `driverquery`: 드라이버 정보를 표시합니다.
- `디스크검사` = `chkdsk`: 디스크를 검사합니다.
- `레지명령` = `reg`: 레지스트리 명령을 실행합니다.
- `부팅디스크검사` = `chkntfs`: 부팅 시 디스크 검사 설정을 관리합니다.
- `부팅편집` = `bcdedit`: 부팅 구성을 편집합니다.
- `사용자관리` = `net user`: 사용자 계정을 관리합니다.
- `서비스제어` = `sc`: 서비스를 조회하거나 제어합니다.
- `연결확인` = `ping`: 네트워크 연결을 확인합니다.
- `연결확장` = `assoc`: 확장자 연결을 표시하거나 설정합니다.
- `열린파일` = `openfiles`: 열린 파일 정보를 표시합니다.
- `이름조회` = `nslookup`: DNS 조회를 수행합니다.
- `작업예약` = `schtasks`: 작업 스케줄러를 관리합니다.
- `접근목록` = `cacls`: 파일/폴더 ACL을 표시하거나 변경합니다.
- `정책결과` = `gpresult`: 그룹 정책 결과를 표시합니다.
- `조회` = `nslookup`: DNS 조회를 수행합니다.
- `주소` = `arp`: 명령을 실행합니다.
- `추적` = `tracert`: 목적지까지의 경로를 추적합니다.
- `파일시스템` = `fsutil`: 파일 시스템 설정을 관리합니다.
- `파일형식` = `ftype`: 파일 형식 연결을 표시하거나 설정합니다.
- `파티션` = `diskpart`: 디스크 파티션을 관리합니다.
- `포트` = `netstat`: 네트워크 연결 상태를 표시합니다.
- `핑` = `ping`: 네트워크 연결을 확인합니다.
- `확인` = `ping`: 네트워크 연결을 확인합니다.

## Windows 도구 실행
- `DirectX진단` = `dxdiag`: Windows 도구를 실행합니다.
- `계산기` = `calc`: Windows 도구를 실행합니다.
- `공유폴더` = `fsmgmt.msc`: Windows 도구를 실행합니다.
- `그룹정책` = `gpedit.msc`: Windows 도구를 실행합니다.
- `그림판` = `mspaint`: Windows 도구를 실행합니다.
- `녹음기` = `soundrecorder`: Windows 도구를 실행합니다.
- `돋보기` = `magnify`: Windows 도구를 실행합니다.
- `디스크관리` = `diskmgmt.msc`: Windows 도구를 실행합니다.
- `레지스트리` = `regedit`: Windows 도구를 실행합니다.
- `로컬사용자` = `lusrmgr.msc`: Windows 도구를 실행합니다.
- `로컬정책` = `secpol.msc`: Windows 도구를 실행합니다.
- `리소스모니터` = `resmon`: Windows 도구를 실행합니다.
- `메모장` = `notepad`: Windows 도구를 실행합니다.
- `문자표` = `charmap`: Windows 도구를 실행합니다.
- `문제기록` = `psr`: Windows 도구를 실행합니다.
- `방화벽` = `wf.msc`: Windows 도구를 실행합니다.
- `백업복원` = `sdclt`: Windows 도구를 실행합니다.
- `사용자계정` = `netplwiz`: Windows 도구를 실행합니다.
- `서비스` = `services.msc`: Windows 도구를 실행합니다.
- `성능모니터` = `perfmon`: Windows 도구를 실행합니다.
- `시스템설정` = `msconfig`: Windows 도구를 실행합니다.
- `시스템속성` = `sysdm.cpl`: Windows 도구를 실행합니다.
- `업데이트` = `control update`: Windows 도구를 실행합니다.
- `원격데스크톱` = `mstsc`: Windows 도구를 실행합니다.
- `윈도우버전` = `winver`: Windows 도구를 실행합니다.
- `이벤트뷰어` = `eventvwr.msc`: Windows 도구를 실행합니다.
- `작업관리자` = `taskmgr`: Windows 도구를 실행합니다.
- `작업스케줄러` = `taskschd.msc`: Windows 도구를 실행합니다.
- `장치관리자` = `devmgmt.msc`: Windows 도구를 실행합니다.
- `제어판` = `control`: Windows 도구를 실행합니다.
- `창문버전` = `winver`: Windows 도구를 실행합니다.
- `캡처도구` = `snippingtool`: Windows 도구를 실행합니다.
- `컴퓨터관리` = `compmgmt.msc`: Windows 도구를 실행합니다.
- `탐색기` = `explorer`: Windows 도구를 실행합니다.
- `터미널` = `wt`: Windows Terminal을 실행합니다.
- `프로그램제거` = `appwiz.cpl`: Windows 도구를 실행합니다.
- `화면키보드` = `osk`: Windows 도구를 실행합니다.

## 외부 개발 도구
- `고` = `go`: 외부 프로그램을 실행합니다.
- `깃` = `git`: 외부 프로그램을 실행합니다.
- `노드` = `node`: 외부 프로그램을 실행합니다.
- `도커` = `docker`: 외부 프로그램을 실행합니다.
- `도커컴포즈` = `docker compose`: 외부 프로그램을 실행합니다.
- `러스트` = `rustc`: 외부 프로그램을 실행합니다.
- `안시블` = `ansible`: 외부 프로그램을 실행합니다.
- `엔피엑스` = `npx`: 외부 프로그램을 실행합니다.
- `엔피엠` = `npm`: 외부 프로그램을 실행합니다.
- `자바` = `java`: 외부 프로그램을 실행합니다.
- `자바컴파일` = `javac`: 외부 프로그램을 실행합니다.
- `카고` = `cargo`: 외부 프로그램을 실행합니다.
- `코드` = `code`: 외부 프로그램을 실행합니다.
- `쿠버` = `kubectl`: 외부 프로그램을 실행합니다.
- `테라폼` = `terraform`: 외부 프로그램을 실행합니다.
- `파이썬` = `python`: 외부 프로그램을 실행합니다.
- `파이썬3` = `python3`: 외부 프로그램을 실행합니다.
- `피아이피` = `pip`: 외부 프로그램을 실행합니다.

## 이스터에그
- `빠른` = `__easter_fast__`: 이스터에그 메시지를 표시합니다.
